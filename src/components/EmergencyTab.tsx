import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const EmergencyTab = () => (
  <Card>
    <CardHeader>
      <CardTitle>Emergency Information</CardTitle>
    </CardHeader>
    <CardContent>
      <p>Emergency contacts and procedures will be displayed here.</p>
    </CardContent>
  </Card>
);