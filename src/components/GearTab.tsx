import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const GearTab = () => (
  <Card>
    <CardHeader>
      <CardTitle>Gear List</CardTitle>
    </CardHeader>
    <CardContent>
      <p>Gear checklist and packing status will be displayed here.</p>
    </CardContent>
  </Card>
);