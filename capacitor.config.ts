import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.merrymantissnap',
  appName: 'merry-mantis-snap',
  webDir: 'dist'
};

export default config;
